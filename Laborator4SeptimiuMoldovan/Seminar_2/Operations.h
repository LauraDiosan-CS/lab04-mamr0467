#pragma once
#include "Film.h"
#include "Repo.h"

//void filterStudents(Film students[], int n, char* nume, int varsta, Film result[], int& resultLen);
//void filterStudentsWithRepo(Repo repo, int n, char* nume, int varsta, Film result[], int& resultLen);